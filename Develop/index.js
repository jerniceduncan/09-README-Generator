// create a variable and require
var questions = require("questions");

// create questions array
const questions = [
    'askProjectName',
    'askProjectVersion',
    'askProjectDescription',
    'askProjectHomepage',
    'askProjectDemoUrl',
    'askAuthorName',
    'askAuthorGithub'
    'askLicenseName',
    'askContributingUrl',
    'askInstallCommand',
    'askUsage',
    'askTestCommand'

];
// 

function writeToFile(index.js, 'questions') {
}

function init() {

}

init();
